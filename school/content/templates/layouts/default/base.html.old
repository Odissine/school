{% load static %}
{% load bootstrap5 %}
{% load i18n %}
{% load auth_extras %}
{% get_current_language as language_code %}
<!DOCTYPE html>
<html lang="fr-FR">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="{% static 'css/bootstrap-icons.css' %}">
    <link rel="stylesheet" href="{% static 'css/bootstrap.min.css' %}">
    <link rel="stylesheet" href="{% static 'css/styles2.css' %}">
    <link href="{% static 'css/select2.min.css' %}" rel="stylesheet" />
    <style>
         @import url('https://fonts.googleapis.com/css2?family=Comfortaa:wght@600&family=Cookie&family=PT+Mono&display=swap');
    </style>

    <title>{% block title %}SCHOOL @ {% endblock %}</title>
</head>
<body class="bg-index">
    <script src="{% static 'js/bootstrap.bundle.min.js' %}"></script>
    <script src="{% static 'js/jquery-3.6.0.min.js' %}"></script>
	<script src="{% static 'js/popper.min.js' %}"></script>
    <script src="{% static 'js/toastr.min.js' %}"></script>
    <script src="{% static 'js/select-menu.js' %}"></script>
    <script src="{% static 'js/select2.min.js' %}"></script>
    <script>
        $(document).ready(function() {
            $('.js-example-basic-single').select2();
        });
    </script>
    <nav class="nav navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <div class="container-fluid">
            <a class="" href="{% url 'index' %}"><img class="" src="{% static 'img/logo.png' %}" width="30" /></a>&nbsp;
            <a class="navbar-brand" href="{% url 'index' %}">ENDTG</a>
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{% url 'index' %}">Accueil</a>
                </li>
                {% if user.is_authenticated %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarScrollingDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Jeux</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
                        <li><a class="dropdown-item" href="{% url 'games:letter' %}">Lettres</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:word_one' %}">Mots</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:addition' %}">Addition</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:addition_posee' %}">Addition Posée</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:soustraction' %}">Soustraction</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:multiplication' %}">Multiplication</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:halo' %}">Halo</a></li>
                    </ul>
                </li>
                {% if request.user|has_group:"ADMIN" or request.user|has_group:"ENSEIGNANT" %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarScrollingDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-expanded="false">Administration</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarScrollingDropdown">
                        <li><a class="dropdown-item" href="{% url 'games:create_word_list' %}">Ajouter des mots</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:create_halo' %}">Créer un halo</a></li>
                        <li><a class="dropdown-item" href="{% url 'account:user-list' 'la' 'wa' %}">Liste des utilisateurs</a></li>
                        <li><a class="dropdown-item" href="{% url 'games:export-words' %}">Export des mots (console)</a></li>
                    </ul>
                </li>
                {% endif %}
                {% endif %}
            </ul>
            {% if user.is_authenticated %}
            <span class="navbar-text">{{ request.user.first_name }} {{ request.user.last_name }}</span>
            <ul class="nav d-flex">
                <li class="nav-item"><a class="nav-link" href="{% url 'account:logout' %}"><button class="btn btn-outline-primary"><i class="bi bi-lock-fill"></i> Se déconnecter</button></a></li>
            </ul>
            {% else %}
            <ul class="nav d-flex">
                <li class="nav-item"><a class="nav-link" href="{% url 'account:register' %}"><button class="btn btn-outline-primary"><i class="bi bi-person-plus-fill"></i> Créer un compte</button></a></li>
                <li class="nav-item"><a class="nav-link" href="{% url 'account:login' %}"><button class="btn btn-outline-primary"><i class="bi bi-unlock-fill"></i> Se connecter</button></a></li>
            </ul>
            {% endif %}
        </div>
    </nav>
    {% if messages %}
    {% for message in messages %}
    <div aria-live="polite" aria-atomic="true" class="bg-dark bd-example-toasts">
        <div class="toast-container position-absolute p-3 top-0 end-0" id="toastPlacement">
            <div class="toast fade hide bg-light" id="error_message">
                <div class="toast-header {% if message.tags %} bg-{{ message.tags }} text-light {% endif %}">
                    <strong class="me-auto">
                        {% if message.tags == 'error' %} Erreur {% else %} Information {% endif %}
                    </strong>
                        {% if message.tags == 'error' %}
                        <i class="bi-exclamation-octagon"></i>
                        {% else %}
                        <i class="bi-info-circle"></i>
                        {% endif %}
                </div>
                <div class="toast-body">{{ message }}</div>
            </div>
        </div>
    </div>
    <script>
        $('#error_message').addClass('show');
        $('#error_message').removeClass('hide');
        function hideMsg () {
            $('#error_message').fadeOut();
            //$('#error_message').removeClass('show');
        }
        setTimeout(hideMsg,3000);
    </script>
    {% endfor %}
    {% endif %}
<div id="content" class="container-fluid py-2 px-4">
{% block content %}
{% endblock %}
</div>
<!-- Function used to shrink nav bar removing paddings and adding black background -->
<script>
    $(window).scroll(function() {
        if ($(document).scrollTop() > 50) {
            $('.nav').addClass('affix');
            console.log("OK");
        } else {
            $('.nav').removeClass('affix');
        }
    });
</script>
</body>
</html>